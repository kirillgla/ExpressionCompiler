Compiler of arithmetic expressions.

The purpose of this project is to create a compiler
that transforms arithmetic expressions into executable code
that prints resuts of evaluation of those expressions
